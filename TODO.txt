bugs
- misses data sometimes when m_wnd->addToTab is done
  sometimes data is thrown away around mainwindow.cpp#1477. Not sure why..
- 3 tests failing
- sometimes pastes the previous thing when pasting from UI. issue with usleep?

misc
- test build on cmake
- add docs on how to package app (make copyq.app) to the readme and hacking files
  ~/Qt5.2.0/5.2.0-rc1/clang_64/bin/qmake CONFIG+=release && make copyq.app 
- b&w themed icon?
- lots more testing!
- sign the bundle (?)
  https://developer.apple.com/library/mac/documentation/Security/Conceptual/CodeSigningGuide/Procedures/Procedures.html#//apple_ref/doc/uid/TP40005929-CH4-SW2
- using a bit of a hack to create the single instance of CopyQPasteboardMime
- CopyQPasteboardMime has the wrong file name
- test all the plugins

macplatform:
- Find a way to get the current window title instead of the application title
